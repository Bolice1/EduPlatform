# Gishoma Multi-School Management System
# Railway Procfile Configuration

# Backend API Service
api: cd backend && npm install && npm run db:init && node src/server.js

# Frontend Web Service (if serving from same dyno, alternative approach)
# web: cd frontend && npm install && npm run build && npm run preview
